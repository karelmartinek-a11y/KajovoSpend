# Reset 2026-02-27

Tato verze je novou vychozi carou po slouceni vsech vetvi z `origin` do `main`.

## Co je zahrnuto
- Slouceni vsech `codex/*` a `copilot/*` vetvi do `main`.
- Stabilizace migracniho testu `test_db_net_gross_migration` (idempotence, timestamp u inseru).
- Doplnene ignore pravidla pro build/test artefakty (`dist/`, `build/KajovoSpend/`, `.pytest_cache/`, `.tmp/`).

## Doporučeno pro vyvojare
1) Cisty setup: `python -m venv .venv && .\\.venv\\Scripts\\activate && pip install -r requirements.txt`.
2) Testy: spust `pytest tests` (rychly sanity check po refaktoru).
3) GUI: `python -m app_gui` (vychozi `config.yaml`/`assets` zustavaji beze zmen).

## Poznamky
- Logy a build artefakty nejsou verzovane; pri debugovani si je nech v lokalnim `LOG/` a `dist/`.
- Pokud delas dalsi velke zmeny, zacni z teto revize (`f05b8da`) a pouzij kratke, izolovane commity.
